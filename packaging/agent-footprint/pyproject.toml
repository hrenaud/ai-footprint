[project]
name = "agent-footprint"
version = "1.0.0"
description = "Paquet coquille : redirige vers ai-footprint (nom retenu du projet)"
requires-python = ">=3.10,<4"
dependencies = ["ai-footprint"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
